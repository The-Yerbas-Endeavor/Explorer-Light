{
  "name": "yerbas-explorer-light",
  "version": "0.1.0",
  "private": true,
  "description": "Database-free RPC-first block explorer for Yerbas",
  "type": "module",
  "engines": {
    "node": ">=18.18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check src/ai.js && node --check src/config.js && node --check src/rpc.js && node --check src/search.js && node --check public/app.js"
  }
}
