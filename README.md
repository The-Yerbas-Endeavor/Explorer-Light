# Yerbas Explorer Light

A lightweight, RPC-first block explorer for Yerbas (YERB).

The blockchain remains the source of truth. Explorer Light talks directly to `yerbasd` over JSON-RPC and does **not** require MongoDB.

Development is in progress on a feature branch.
